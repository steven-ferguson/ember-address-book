<div class='row'>
  <h1 class='text-center'>Contacts</h1>
</div>
<div class='row'>
  <div class="small-4 columns">
    <ul class="side-nav">
      <li>{{#link-to 'contacts.new' id='new-contact'}}Add a new contact{{/link-to}}</li>
      {{#each}}
        <li class='contact'>{{#link-to 'contact' this}}{{name}}{{/link-to}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="small-8 columns">
    {{outlet}}
  </div>
</div>